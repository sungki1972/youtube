# MP4 요약 기능 - Supabase SQL

Supabase 프로젝트는 anon key로 접속하므로 서버에서 DDL을 실행할 수 없습니다.
아래 SQL을 **Supabase 대시보드 → SQL Editor**에서 한 번 실행해 주세요.

> 실행하지 않아도 서버는 동작합니다. `mp4_summary` 테이블이 없으면 서버가 자동으로
> `mp4/index.json` 로컬 파일에 기록(폴백)하고, MP4 탭 목록도 로컬 파일에서 읽어옵니다.
> 단, 여러 기기/재배포 간 공유·영속성을 위해 테이블 생성을 권장합니다.

```sql
-- 1) MP4 요약 게시판 테이블
create table if not exists mp4_summary (
  id bigint generated by default as identity primary key,
  title text,
  youtube_url text,               -- 원본 유튜브 URL
  original_file text,             -- mp4/ 아래 원본 파일명
  summary_file text,              -- mp4/ 아래 요약 파일명
  duration_sec integer,           -- 원본 길이(초)
  summary_duration_sec integer,   -- 요약 길이(초)
  youtube_video_id text,          -- 업로드된 유튜브 영상 ID
  youtube_error text,             -- 유튜브 업로드 실패 시 메시지(파이프라인은 계속 성공)
  status text default 'processing', -- processing | completed | failed
  error text,                     -- 처리 실패 시 메시지
  summary_id bigint,              -- 연결된 youtube_summary.id (있을 경우)
  created_at timestamptz default now()
);

create index if not exists idx_mp4_summary_created_at on mp4_summary(created_at desc);

-- 2) youtube_summary에 원본 URL 컬럼 추가
alter table youtube_summary add column if not exists url text;
```

## 참고: 삭제된 것

- `openai_result` 테이블은 더 이상 사용하지 않습니다. (OPENAI 탭 → MP4 탭으로 대체)
  기존 데이터를 정리하려면 아래를 실행할 수 있습니다(선택).

```sql
-- (선택) 더 이상 사용하지 않는 테이블 정리
-- drop table if exists openai_result;
```

- `youtube_summary.youyak`(요약) 컬럼은 더 이상 생성/조회하지 않습니다.
  이 컬럼에 NOT NULL 제약이 있어 서버는 빈 문자열('')로 채워 저장합니다.
  깔끔하게 정리하려면 아래 중 하나를 실행할 수 있습니다(선택).

```sql
-- (선택 1) NOT NULL 제약만 해제
-- alter table youtube_summary alter column youyak drop not null;

-- (선택 2) youyak 컬럼 자체를 제거 (서버가 자동으로 youyak 없이 저장하도록 폴백함)
-- alter table youtube_summary drop column if exists youyak;
```
